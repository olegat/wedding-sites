#include "burger.css"
#include "language.css"
#include "stamp.css"

/* Reset some default styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --airmail-stripe-color1: #d22f47;
    --airmail-stripe-color2: #315f9b;
    --airmail-stripe-width: 2.4rem;
    --airmail-background: #eee6d3;
    --color-light: rgb(237, 232, 226);
    --color-mid: rgb(179, 178, 158);
    --color-dark: rgb(41, 55, 39);
    background: var(--color-mid);
}

.banner-section {
    background: var(--color-mid);
    display: flex;
    flex-direction: column;
    align-items: center;
}

.airmail-border {
    padding: var(--airmail-stripe-width);
    border-image-source: repeating-linear-gradient(
        142deg,
        var(--airmail-stripe-color1) 0px 30px,
        transparent 30px 50px,
        var(--airmail-stripe-color2) 50px 80px,
        transparent 80px 100px
    );
    border-image-slice: 40;
    border-image-width: var(--airmail-stripe-width);
    background-color: var(--airmail-background);
}

.airmail-positioning {
    margin: 2rem;
    aspect-ratio: 2.3 / 1;
    width: 85vw;
}

.airmail-positioning > * {
    position: absolute;
}

.airmail-positioning .stamp-border {
    --stamp-border-radius: 0.25vw;
    width: 15vw;
}
